

namespace ApiTests
{
    public class GetUrlFromSlugTest
    {
        public void ShortUrlIsReturnedFromGivenSlug()
        {
            // Pass in a slug and get one returned
        }


        public void ErrorThrownIfSlugIsNotPassedIn()
        {
            // Test to make sure error is thrown if slug is not passed in
        }

        public void ReturnsNullShortUrlIfSlugDoesNotExist()
        {
            // If slug doesn't exist, assert it returns a null url with correct message
        }
    }
}
